<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="ooo.klae.connex.backend.mappers.AiWorkspaceGovernanceMapper">
    <resultMap id="governanceMap" type="ooo.klae.connex.backend.beans.AiWorkspaceGovernance">
        <id property="workspaceId" column="workspace_id" />
        <result property="aiEnabled" column="ai_enabled" />
        <result property="assistantMaxSteps" column="assistant_max_steps" />
    </resultMap>

    <select id="get" resultMap="governanceMap">
        SELECT workspace_id, ai_enabled, assistant_max_steps
        FROM ai_workspace_governance
        WHERE workspace_id = #{workspaceId}
    </select>

    <insert id="upsert">
        INSERT INTO ai_workspace_governance
            (workspace_id, ai_enabled, assistant_max_steps)
        VALUES
            (#{workspaceId}, #{enabled}, #{assistantMaxSteps})
        ON DUPLICATE KEY UPDATE
            ai_enabled = VALUES(ai_enabled),
            assistant_max_steps = VALUES(assistant_max_steps)
    </insert>
</mapper>
