<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="ooo.klae.connex.backend.mappers.DealDocumentMapper">

  <resultMap id="DealDocumentResult" type="DealDocument">
    <id     property="id"          column="id"/>
    <result property="workspaceId" column="workspace_id"/>
    <result property="dealId"      column="deal_id"/>
    <result property="templateId"  column="template_id"/>
    <result property="type"        column="type"/>
    <result property="locale"      column="locale"/>
    <result property="status"      column="status"/>
    <result property="version"     column="version"/>
    <result property="title"       column="title"/>
    <result property="content"     column="content"/>
    <result property="currency"    column="currency"/>
    <result property="generatedAt" column="generated_at"/>
    <result property="createdBy"   column="created_by"/>
    <result property="createdAt"   column="created_at"/>
    <result property="updatedAt"   column="updated_at"/>
  </resultMap>

  <sql id="cols">
    id, workspace_id, deal_id, template_id, type, locale, status, version, title,
    content, currency, generated_at, created_by, created_at, updated_at
  </sql>

  <select id="getByDealId" resultMap="DealDocumentResult">
    SELECT <include refid="cols"/> FROM deal_document
    WHERE workspace_id = #{workspaceId} AND deal_id = #{dealId}
    ORDER BY version DESC, id DESC
  </select>

  <!-- Cross-deal index projection. The parent deal is joined for its name and owner: a generated
       document has no owner column of its own, so deal ownership is the only ownership the index
       can scope by. No control-plane join here — creator display names are resolved client-side
       from the member list, as the per-deal document read already does. -->
  <sql id="summaryColumns">
    dd.id, dd.deal_id, d.name AS deal_name, d.owner_id AS deal_owner_id, dd.type, dd.status,
    dd.version, dd.title, dd.currency, dd.created_by, dd.generated_at, dd.created_at, dd.updated_at
  </sql>

  <sql id="workspaceFilter">
    WHERE dd.workspace_id = #{workspaceId}
    <if test="query != null">
      AND (dd.title LIKE #{query} OR d.name LIKE #{query})
    </if>
    <if test="statuses != null and statuses.size() > 0">
      AND dd.status IN
      <foreach collection="statuses" item="status" open="(" separator="," close=")">#{status}</foreach>
    </if>
    <if test="types != null and types.size() > 0">
      AND dd.type IN
      <foreach collection="types" item="type" open="(" separator="," close=")">#{type}</foreach>
    </if>
    <if test="dealId != null">
      AND dd.deal_id = #{dealId}
    </if>
    <choose>
      <when test="memberScope.mode.name() == 'ME'">
        AND d.owner_id = #{memberScope.userId}
      </when>
      <when test="memberScope.mode.name() == 'MEMBERS'">
        AND d.owner_id IN
        <foreach item="id" collection="memberScope.memberIds" open="(" separator="," close=")">#{id}</foreach>
      </when>
      <when test="memberScope.mode.name() == 'UNASSIGNED'">
        AND d.owner_id IS NULL
      </when>
    </choose>
  </sql>

  <select id="getWorkspacePage" resultType="ooo.klae.connex.backend.dto.GeneratedDocumentSummaryDto">
    SELECT <include refid="summaryColumns"/>
    FROM deal_document dd
    JOIN deal d ON d.workspace_id = dd.workspace_id AND d.id = dd.deal_id
    <include refid="workspaceFilter"/>
    ORDER BY dd.generated_at DESC, dd.id DESC
    LIMIT #{limit} OFFSET #{offset}
  </select>

  <select id="countWorkspace" resultType="long">
    SELECT COUNT(*)
    FROM deal_document dd
    JOIN deal d ON d.workspace_id = dd.workspace_id AND d.id = dd.deal_id
    <include refid="workspaceFilter"/>
  </select>

  <select id="search" resultType="ooo.klae.connex.backend.dto.GeneratedDocumentSummaryDto">
    SELECT <include refid="summaryColumns"/>
    FROM deal_document dd
    JOIN deal d ON d.workspace_id = dd.workspace_id AND d.id = dd.deal_id
    WHERE dd.workspace_id = #{workspaceId}
      AND (dd.title LIKE #{query} OR d.name LIKE #{query})
    ORDER BY dd.generated_at DESC, dd.id DESC
    LIMIT 10
  </select>

  <select id="getByIds" resultMap="DealDocumentResult">
    SELECT <include refid="cols"/> FROM deal_document
    WHERE workspace_id = #{workspaceId}
      AND id IN
      <foreach collection="ids" item="id" open="(" separator="," close=")">#{id}</foreach>
  </select>

  <select id="getById" resultMap="DealDocumentResult">
    SELECT <include refid="cols"/> FROM deal_document
    WHERE workspace_id = #{workspaceId} AND id = #{id}
  </select>

  <select id="lockById" resultMap="DealDocumentResult">
    SELECT <include refid="cols"/> FROM deal_document
    WHERE workspace_id = #{workspaceId} AND id = #{id}
    FOR UPDATE
  </select>

  <select id="findDealIdById" resultType="java.lang.Integer">
    SELECT deal_id FROM deal_document
    WHERE workspace_id = #{workspaceId} AND id = #{id}
  </select>

  <select id="maxVersion" resultType="java.lang.Integer">
    SELECT MAX(version) FROM deal_document WHERE workspace_id = #{workspaceId} AND deal_id = #{dealId}
  </select>

  <select id="countNonDraftByDeal" resultType="int">
    SELECT COUNT(*) FROM deal_document
    WHERE workspace_id = #{workspaceId}
      AND deal_id = #{dealId}
      AND status &lt;&gt; 'draft'
  </select>

  <insert id="insert" useGeneratedKeys="true" keyProperty="id">
    INSERT INTO deal_document (
      workspace_id, deal_id, template_id, type, locale, status, version, title, content, currency, created_by
    ) VALUES (
      #{workspaceId}, #{dealId}, #{templateId}, #{type}, #{locale}, #{status}, #{version}, #{title},
      #{content}, #{currency}, #{createdBy}
    )
  </insert>

  <update id="updateStatus">
    UPDATE deal_document SET status = #{status}
    WHERE workspace_id = #{workspaceId} AND id = #{id}
  </update>

  <delete id="delete">
    DELETE FROM deal_document WHERE workspace_id = #{workspaceId} AND id = #{id}
  </delete>
</mapper>
