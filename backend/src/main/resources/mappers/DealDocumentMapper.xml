<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="ooo.klae.connex.backend.mappers.DealDocumentMapper">

  <resultMap id="DealDocumentResult" type="DealDocument">
    <id     property="id"          column="id"/>
    <result property="workspaceId" column="workspace_id"/>
    <result property="dealId"      column="deal_id"/>
    <result property="templateId"  column="template_id"/>
    <result property="type"        column="type"/>
    <result property="locale"      column="locale"/>
    <result property="status"      column="status"/>
    <result property="version"     column="version"/>
    <result property="title"       column="title"/>
    <result property="content"     column="content"/>
    <result property="currency"    column="currency"/>
    <result property="generatedAt" column="generated_at"/>
    <result property="createdBy"   column="created_by"/>
    <result property="createdAt"   column="created_at"/>
    <result property="updatedAt"   column="updated_at"/>
  </resultMap>

  <sql id="cols">
    id, workspace_id, deal_id, template_id, type, locale, status, version, title,
    content, currency, generated_at, created_by, created_at, updated_at
  </sql>

  <select id="getByDealId" resultMap="DealDocumentResult">
    SELECT <include refid="cols"/> FROM deal_document
    WHERE workspace_id = #{workspaceId} AND deal_id = #{dealId}
    ORDER BY version DESC, id DESC
  </select>

  <select id="getById" resultMap="DealDocumentResult">
    SELECT <include refid="cols"/> FROM deal_document
    WHERE workspace_id = #{workspaceId} AND id = #{id}
  </select>

  <select id="lockById" resultMap="DealDocumentResult">
    SELECT <include refid="cols"/> FROM deal_document
    WHERE workspace_id = #{workspaceId} AND id = #{id}
    FOR UPDATE
  </select>

  <select id="maxVersion" resultType="java.lang.Integer">
    SELECT MAX(version) FROM deal_document WHERE workspace_id = #{workspaceId} AND deal_id = #{dealId}
  </select>

  <insert id="insert" useGeneratedKeys="true" keyProperty="id">
    INSERT INTO deal_document (
      workspace_id, deal_id, template_id, type, locale, status, version, title, content, currency, created_by
    ) VALUES (
      #{workspaceId}, #{dealId}, #{templateId}, #{type}, #{locale}, #{status}, #{version}, #{title},
      #{content}, #{currency}, #{createdBy}
    )
  </insert>

  <update id="updateStatus">
    UPDATE deal_document SET status = #{status}
    WHERE workspace_id = #{workspaceId} AND id = #{id}
  </update>

  <delete id="delete">
    DELETE FROM deal_document WHERE workspace_id = #{workspaceId} AND id = #{id}
  </delete>
</mapper>
