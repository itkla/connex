<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="ooo.klae.connex.backend.mappers.TagMapper">

  <resultMap id="TagFullResult" type="Tag">
    <id property="id" column="id"/>
    <result property="workspaceId" column="workspace_id"/>
    <result property="name" column="name"/>
    <result property="color" column="color"/>
    <collection property="people" ofType="Person" column="{workspaceId=workspace_id,tagId=id}" select="ooo.klae.connex.backend.mappers.PersonMapper.getPersonsByTagId"/>
    <collection property="companies" ofType="Company" column="{workspaceId=workspace_id,tagId=id}" select="ooo.klae.connex.backend.mappers.CompanyMapper.getCompaniesByTagId"/>
  </resultMap>

  <select id="getAllTags" resultType="Tag">
    SELECT id, workspace_id, name, color FROM tag WHERE workspace_id = #{workspaceId} ORDER BY name
  </select>

  <select id="getTagById" resultMap="TagFullResult">
    SELECT id, workspace_id, name, color FROM tag WHERE workspace_id = #{workspaceId} AND id = #{id}
  </select>

  <select id="getTagByName" resultType="Tag">
    SELECT id, workspace_id, name, color FROM tag WHERE workspace_id = #{workspaceId} AND name = #{name}
  </select>

  <select id="exists" resultType="boolean">
    SELECT EXISTS(
      SELECT 1 FROM tag WHERE workspace_id = #{workspaceId} AND id = #{id}
    )
  </select>

  <select id="search" resultType="Tag">
    SELECT id, workspace_id, name, color FROM tag
    WHERE workspace_id = #{workspaceId} AND name LIKE #{query}
    ORDER BY name
    LIMIT 10
  </select>

  <insert id="insert" useGeneratedKeys="true" keyProperty="id">
    INSERT INTO tag (workspace_id, name, color) VALUES (#{workspaceId}, #{name}, #{color})
  </insert>

  <update id="update">
    UPDATE tag SET name = #{name}, color = #{color} WHERE workspace_id = #{workspaceId} AND id = #{id}
  </update>

  <delete id="delete">DELETE FROM tag WHERE workspace_id = #{workspaceId} AND id = #{id}</delete>

  <select id="getTagsByPersonId" resultType="Tag">
    SELECT t.id, t.workspace_id, t.name, t.color
    FROM tag t
    JOIN person_tag pt ON pt.tag_id = t.id
    WHERE pt.person_id = #{personId}
    ORDER BY t.name
  </select>

  <select id="getTagsByCompanyId" resultType="Tag">
    SELECT t.id, t.workspace_id, t.name, t.color
    FROM tag t
    JOIN company_tag ct ON ct.tag_id = t.id
    WHERE ct.company_id = #{companyId}
    ORDER BY t.name
  </select>

  <select id="getTagsByDealId" resultType="Tag">
    SELECT t.id, t.workspace_id, t.name, t.color
    FROM tag t
    JOIN deal_tag dt ON dt.tag_id = t.id
    WHERE dt.deal_id = #{dealId}
    ORDER BY t.name
  </select>

  <select id="getTagsByAttachmentId" resultType="Tag">
    SELECT t.id, t.workspace_id, t.name, t.color
    FROM tag t
    JOIN attachment_tag at ON at.tag_id = t.id
    WHERE at.attachment_id = #{attachmentId}
    ORDER BY t.name
  </select>
</mapper>
