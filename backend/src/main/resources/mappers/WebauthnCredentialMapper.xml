<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="ooo.klae.connex.backend.mappers.WebauthnCredentialMapper">

  <sql id="cols">
    id, credential_id, user_entity_user_id, credential_type, public_key, signature_count,
    uv_initialized, backup_eligible, backup_state, transports, attestation_object,
    attestation_client_data_json, label, created_at, last_used_at
  </sql>

  <select id="findByCredentialId" resultType="ooo.klae.connex.backend.webauthn.WebauthnCredentialRow">
    SELECT <include refid="cols"/> FROM webauthn_credential WHERE credential_id = #{credentialId}
  </select>

  <select id="findByUserEntityUserId" resultType="ooo.klae.connex.backend.webauthn.WebauthnCredentialRow">
    SELECT <include refid="cols"/> FROM webauthn_credential
    WHERE user_entity_user_id = #{userEntityUserId}
    ORDER BY created_at
  </select>

  <select id="findByUserEntityUserIdForUpdate" resultType="ooo.klae.connex.backend.webauthn.WebauthnCredentialRow">
    SELECT <include refid="cols"/> FROM webauthn_credential
    WHERE user_entity_user_id = #{userEntityUserId}
    ORDER BY created_at
    FOR UPDATE
  </select>

  <insert id="insert" useGeneratedKeys="true" keyProperty="id">
    INSERT INTO webauthn_credential
      (credential_id, user_entity_user_id, credential_type, public_key, signature_count,
       uv_initialized, backup_eligible, backup_state, transports, attestation_object,
       attestation_client_data_json, label, created_at, last_used_at)
    VALUES
      (#{credentialId}, #{userEntityUserId}, #{credentialType}, #{publicKey}, #{signatureCount},
       #{uvInitialized}, #{backupEligible}, #{backupState}, #{transports}, #{attestationObject},
       #{attestationClientDataJson}, #{label}, #{createdAt}, #{lastUsedAt})
  </insert>

  <update id="updateMutable">
    UPDATE webauthn_credential
    SET signature_count = #{signatureCount},
        uv_initialized = #{uvInitialized},
        backup_state = #{backupState},
        last_used_at = #{lastUsedAt},
        label = #{label}
    WHERE credential_id = #{credentialId}
  </update>

  <update id="updateLabel">
    UPDATE webauthn_credential SET label = #{label} WHERE credential_id = #{credentialId}
  </update>

  <delete id="delete">DELETE FROM webauthn_credential WHERE credential_id = #{credentialId}</delete>
</mapper>
