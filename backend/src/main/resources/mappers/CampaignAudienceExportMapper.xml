<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="ooo.klae.connex.backend.mappers.CampaignAudienceExportMapper">

  <resultMap id="CampaignAudienceExportResult" type="CampaignAudienceExport">
    <id property="id" column="id"/>
    <result property="workspaceId" column="workspace_id"/>
    <result property="campaignId" column="campaign_id"/>
    <result property="snapshotId" column="snapshot_id"/>
    <result property="connector" column="connector"/>
    <result property="externalListId" column="external_list_id"/>
    <result property="idempotencyKey" column="idempotency_key"/>
    <result property="frozenMemberIdsJson" column="frozen_member_ids_json"/>
    <result property="pushedMemberIdsJson" column="pushed_member_ids_json"/>
    <result property="status" column="status"/>
    <result property="attempt" column="attempt"/>
    <result property="leaseUntil" column="lease_until"/>
    <result property="reconciliationRequiredAt" column="reconciliation_required_at"/>
    <result property="failureReason" column="failure_reason"/>
    <result property="outcomeClassification" column="outcome_classification"/>
    <result property="lateOutcome" column="late_outcome"/>
    <result property="totalMembers" column="total_members"/>
    <result property="pushedCount" column="pushed_count"/>
    <result property="failedCount" column="failed_count"/>
    <result property="createdById" column="created_by_id"/>
    <result property="createdAt" column="created_at"/>
    <result property="updatedAt" column="updated_at"/>
  </resultMap>

  <sql id="columns">
    id, workspace_id, campaign_id, snapshot_id, connector, external_list_id, idempotency_key,
    frozen_member_ids_json, pushed_member_ids_json, status, attempt, lease_until,
    reconciliation_required_at, failure_reason, outcome_classification, late_outcome,
    total_members, pushed_count, failed_count, created_by_id, created_at, updated_at
  </sql>

  <sql id="historyColumns">
    id, workspace_id, campaign_id, snapshot_id, connector, external_list_id, idempotency_key,
    frozen_member_ids_json, pushed_member_ids_json, status, attempt, lease_until,
    CASE
      WHEN reconciliation_required_at IS NOT NULL THEN reconciliation_required_at
      WHEN status = 'running' AND lease_until &lt; UTC_TIMESTAMP(6) THEN lease_until
      ELSE NULL
    END AS reconciliation_required_at,
    failure_reason, outcome_classification, late_outcome,
    total_members, pushed_count, failed_count,
    created_by_id, created_at, updated_at
  </sql>

  <select id="getByCampaign" resultMap="CampaignAudienceExportResult">
    SELECT <include refid="historyColumns"/>
    FROM campaign_audience_export
    WHERE workspace_id = #{workspaceId} AND campaign_id = #{campaignId}
    ORDER BY id DESC
  </select>

  <select id="getExport" resultMap="CampaignAudienceExportResult">
    SELECT <include refid="historyColumns"/>
    FROM campaign_audience_export
    WHERE workspace_id = #{workspaceId} AND id = #{id}
  </select>

  <select id="getExportForUpdate" resultMap="CampaignAudienceExportResult">
    SELECT <include refid="columns"/>
    FROM campaign_audience_export
    WHERE workspace_id = #{workspaceId} AND id = #{id}
    FOR UPDATE
  </select>

  <select id="existsActiveForSnapshotConnector" resultType="boolean">
    SELECT EXISTS (
      SELECT 1
      FROM campaign_audience_export
      WHERE workspace_id = #{workspaceId}
        AND campaign_id = #{campaignId}
        AND snapshot_id = #{snapshotId}
        AND connector = #{connector}
        AND (
          reconciliation_required_at IS NOT NULL
          OR status IN ('draft', 'running', 'completed')
        )
    )
  </select>

  <update id="markStaleRunningNeedsReconciliation">
    UPDATE campaign_audience_export
    SET reconciliation_required_at = UTC_TIMESTAMP(6),
        lease_until = NULL,
        outcome_classification = 'ambiguous'
    WHERE workspace_id = #{workspaceId}
      AND campaign_id = #{campaignId}
      AND id IN
      <foreach collection="exportIds" item="exportId" open="(" separator="," close=")">
        #{exportId}
      </foreach>
      AND status = 'running'
      AND reconciliation_required_at IS NULL
      AND lease_until &lt; UTC_TIMESTAMP(6)
  </update>

  <select id="nextAttemptForSnapshotTarget" resultType="int">
    SELECT COUNT(*) + 1
    FROM campaign_audience_export
    WHERE workspace_id = #{workspaceId}
      AND campaign_id = #{campaignId}
      AND snapshot_id = #{snapshotId}
      AND connector = #{connector}
      AND external_list_id = #{externalListId}
  </select>

  <insert id="insertExport" useGeneratedKeys="true" keyProperty="export.id">
    INSERT INTO campaign_audience_export
      (workspace_id, campaign_id, snapshot_id, connector, external_list_id, idempotency_key,
       frozen_member_ids_json, pushed_member_ids_json, status, attempt, lease_until,
       reconciliation_required_at, failure_reason, outcome_classification, late_outcome,
       total_members, pushed_count, failed_count, created_by_id)
    VALUES
      (#{export.workspaceId}, #{export.campaignId}, #{export.snapshotId}, #{export.connector},
       #{export.externalListId}, #{export.idempotencyKey},
       #{export.frozenMemberIdsJson}, #{export.pushedMemberIdsJson},
       #{export.status}, #{export.attempt},
       CASE
         WHEN #{export.status} = 'running' AND #{export.reconciliationRequiredAt} IS NULL
           THEN DATE_ADD(UTC_TIMESTAMP(6), INTERVAL #{leaseMicros} MICROSECOND)
         ELSE NULL
       END,
       #{export.reconciliationRequiredAt}, #{export.failureReason},
       #{export.outcomeClassification}, #{export.lateOutcome},
       #{export.totalMembers}, #{export.pushedCount}, #{export.failedCount}, #{export.createdById})
  </insert>

  <update id="stagePush">
    UPDATE campaign_audience_export
    SET external_list_id = #{export.externalListId},
        idempotency_key = #{export.idempotencyKey},
        pushed_member_ids_json = #{export.pushedMemberIdsJson},
        attempt = #{export.attempt},
        failed_count = #{export.failedCount},
        lease_until = DATE_ADD(UTC_TIMESTAMP(6), INTERVAL #{leaseMicros} MICROSECOND)
    WHERE workspace_id = #{export.workspaceId}
      AND id = #{export.id}
      AND status = 'running'
      AND reconciliation_required_at IS NULL
  </update>

  <update id="updateOutcome">
    UPDATE campaign_audience_export
    SET external_list_id = #{externalListId},
        status = #{status},
        total_members = #{totalMembers},
        pushed_count = #{pushedCount},
        failed_count = #{failedCount},
        lease_until = NULL,
        reconciliation_required_at = CASE
          WHEN #{status} = 'running' THEN UTC_TIMESTAMP(6)
          ELSE NULL
        END,
        failure_reason = #{failureReason},
        outcome_classification = #{outcomeClassification}
    WHERE workspace_id = #{workspaceId}
      AND id = #{id}
      AND status = 'running'
      AND reconciliation_required_at IS NULL
      AND attempt = #{attempt}
      AND idempotency_key &lt;=&gt; #{idempotencyKey}
  </update>

  <update id="markLateOutcome">
    UPDATE campaign_audience_export
    SET late_outcome = #{lateOutcome},
        failure_reason = COALESCE(failure_reason, #{failureReason})
    WHERE workspace_id = #{workspaceId}
      AND id = #{id}
      AND late_outcome IS NULL
  </update>

  <update id="resolveReconciliation">
    UPDATE campaign_audience_export
    SET status = #{status},
        pushed_count = #{pushedCount},
        failed_count = #{failedCount},
        lease_until = NULL,
        reconciliation_required_at = NULL,
        outcome_classification = #{outcomeClassification}
    WHERE workspace_id = #{workspaceId}
      AND id = #{id}
      AND status = 'running'
      AND lease_until IS NULL
  </update>

</mapper>
