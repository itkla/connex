<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="ooo.klae.connex.backend.mappers.PrivilegedMfaRecoveryRedemptionMapper">

  <insert id="insertIfAbsent">
    INSERT IGNORE INTO privileged_mfa_recovery_redemption (token_digest, user_id, operator, redeemed_at)
    VALUES (#{tokenDigest}, #{userId}, #{operator}, UTC_TIMESTAMP(6))
  </insert>
</mapper>
