<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="ooo.klae.connex.backend.mappers.LegacyControlUploadMigrationMapper">

  <select id="findWorkspaceIds" resultType="int">
    SELECT id
    FROM workspace
    WHERE id &gt; #{afterId}
    ORDER BY id
    LIMIT #{limit}
  </select>

  <select id="findUserImages" resultType="ooo.klae.connex.backend.storage.LegacyUploadRecord">
    SELECT id, profile_picture_url AS url
    FROM app_user
    WHERE id &gt; #{afterId} AND profile_picture_url LIKE '/profile-pictures/%'
    ORDER BY id
    LIMIT #{limit}
  </select>

  <update id="updateUserImage">
    UPDATE app_user
    SET profile_picture_url = #{newUrl}
    WHERE id = #{id} AND profile_picture_url &lt;=&gt; #{currentUrl}
  </update>

  <select id="countUserReferences" resultType="int">
    SELECT COUNT(*)
    FROM app_user
    WHERE profile_picture_url LIKE '/profile-pictures/%'
  </select>

</mapper>
