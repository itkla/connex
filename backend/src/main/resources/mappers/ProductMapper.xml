<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="ooo.klae.connex.backend.mappers.ProductMapper">

  <resultMap id="ProductResult" type="Product">
    <id     property="id"               column="id"/>
    <result property="workspaceId"      column="workspace_id"/>
    <result property="sku"              column="sku"/>
    <result property="name"             column="name"/>
    <result property="description"      column="description"/>
    <result property="active"           column="active"/>
    <result property="unit"             column="unit"/>
    <result property="unitPrice"        column="unit_price"/>
    <result property="currency"         column="currency"/>
    <result property="taxRate"          column="tax_rate"/>
    <result property="billingFrequency" column="billing_frequency"/>
    <result property="effectiveStart"   column="effective_start"/>
    <result property="effectiveEnd"     column="effective_end"/>
    <result property="createdAt"        column="created_at"/>
    <result property="updatedAt"        column="updated_at"/>
  </resultMap>

  <sql id="cols">
    id, workspace_id, sku, name, description, active, unit, unit_price, currency,
    tax_rate, billing_frequency, effective_start, effective_end, created_at, updated_at
  </sql>

  <select id="getFiltered" resultMap="ProductResult">
    SELECT <include refid="cols"/> FROM product
    WHERE workspace_id = #{workspaceId}
    <if test="query != null">
      AND (
        LOWER(name) COLLATE utf8mb4_bin LIKE LOWER(#{query}) COLLATE utf8mb4_bin
        OR LOWER(sku) COLLATE utf8mb4_bin LIKE LOWER(#{query}) COLLATE utf8mb4_bin
        OR LOWER(description) COLLATE utf8mb4_bin LIKE LOWER(#{query}) COLLATE utf8mb4_bin
      )
    </if>
    ORDER BY name, id
  </select>

  <select id="getById" resultMap="ProductResult">
    SELECT <include refid="cols"/> FROM product
    WHERE workspace_id = #{workspaceId} AND id = #{id}
  </select>

  <!-- Catalog-import SKU lookup. Deliberately NOT binary-collated (unlike getFiltered): the
       classification must agree with uq_product_workspace_sku, or a preview would report "new"
       for a SKU the unique index then rejects on insert. -->
  <select id="findBySkus" resultMap="ProductResult">
    SELECT <include refid="cols"/> FROM product
    WHERE workspace_id = #{workspaceId}
      AND sku IN
      <foreach collection="skus" item="sku" open="(" separator="," close=")">#{sku}</foreach>
    ORDER BY id
  </select>

  <select id="getByIdForUpdate" resultMap="ProductResult">
    SELECT <include refid="cols"/> FROM product
    WHERE workspace_id = #{workspaceId} AND id = #{id}
    FOR UPDATE
  </select>

  <select id="exists" resultType="boolean">
    SELECT EXISTS(SELECT 1 FROM product WHERE workspace_id = #{workspaceId} AND id = #{id})
  </select>

  <insert id="insert" useGeneratedKeys="true" keyProperty="id">
    INSERT INTO product (
      workspace_id, sku, name, description, active, unit, unit_price, currency,
      tax_rate, billing_frequency, effective_start, effective_end
    ) VALUES (
      #{workspaceId}, #{sku}, #{name}, #{description}, #{active}, #{unit}, #{unitPrice}, #{currency},
      #{taxRate}, #{billingFrequency}, #{effectiveStart}, #{effectiveEnd}
    )
  </insert>

  <!-- Explicit jdbcType on every nullable column: MyBatis cannot infer a type for a null bound
       inside a <foreach>, so an all-null batch column would otherwise fail at bind time. -->
  <insert id="insertBatch" useGeneratedKeys="true" keyProperty="id">
    INSERT INTO product (
      workspace_id, sku, name, description, active, unit, unit_price, currency,
      tax_rate, billing_frequency, effective_start, effective_end
    ) VALUES
    <foreach collection="list" item="p" separator=",">
      (#{p.workspaceId}, #{p.sku}, #{p.name}, #{p.description,jdbcType=VARCHAR}, #{p.active},
       #{p.unit,jdbcType=VARCHAR}, #{p.unitPrice}, #{p.currency}, #{p.taxRate,jdbcType=DECIMAL},
       #{p.billingFrequency}, #{p.effectiveStart,jdbcType=DATE}, #{p.effectiveEnd,jdbcType=DATE})
    </foreach>
  </insert>

  <update id="update">
    UPDATE product SET
      sku = #{sku}, name = #{name}, description = #{description}, active = #{active},
      unit = #{unit}, unit_price = #{unitPrice}, currency = #{currency}, tax_rate = #{taxRate},
      billing_frequency = #{billingFrequency}, effective_start = #{effectiveStart},
      effective_end = #{effectiveEnd}
    WHERE workspace_id = #{workspaceId} AND id = #{id}
  </update>

  <delete id="delete">
    DELETE FROM product WHERE workspace_id = #{workspaceId} AND id = #{id}
  </delete>
</mapper>
