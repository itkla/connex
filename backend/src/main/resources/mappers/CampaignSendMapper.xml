<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="ooo.klae.connex.backend.mappers.CampaignSendMapper">

  <sql id="sendColumns">
    id, workspace_id, campaign_id, snapshot_id, origin, message_id, message_version, channel, purpose,
    provider_id, status, scheduled_at, started_at, completed_at, total_recipients,
    dispatched_count, skipped_count, failed_count, created_by_id, created_at, updated_at
  </sql>

  <select id="getSendsByCampaign" resultType="CampaignSend">
    SELECT <include refid="sendColumns"/> FROM campaign_send
    WHERE workspace_id = #{workspaceId} AND campaign_id = #{campaignId}
    ORDER BY id DESC
  </select>

  <select id="getSend" resultType="CampaignSend">
    SELECT <include refid="sendColumns"/> FROM campaign_send
    WHERE workspace_id = #{workspaceId} AND id = #{id}
  </select>

  <select id="getSendForUpdate" resultType="CampaignSend">
    SELECT <include refid="sendColumns"/> FROM campaign_send
    WHERE workspace_id = #{workspaceId} AND id = #{id}
    FOR UPDATE
  </select>

  <select id="getTriggeredSend" resultType="CampaignSend">
    SELECT <include refid="sendColumns"/> FROM campaign_send
    WHERE workspace_id = #{workspaceId}
      AND message_id = #{messageId}
      AND triggered_message_version = #{messageVersion}
  </select>

  <insert id="insertSend" useGeneratedKeys="true" keyProperty="id">
    INSERT INTO campaign_send
      (workspace_id, campaign_id, snapshot_id, origin, message_id, message_version, channel, purpose,
       provider_id, status, scheduled_at, total_recipients, created_by_id)
    VALUES
      (#{workspaceId}, #{campaignId}, #{snapshotId}, COALESCE(#{origin}, 'audience'),
       #{messageId}, #{messageVersion}, #{channel},
       #{purpose}, #{providerId}, #{status}, #{scheduledAt}, #{totalRecipients}, #{createdById})
  </insert>

  <update id="transitionStatus">
    UPDATE campaign_send SET status = #{toStatus}
    WHERE workspace_id = #{workspaceId} AND id = #{id} AND status = #{fromStatus}
  </update>

  <update id="assignProvider">
    UPDATE campaign_send SET provider_id = #{providerId}
    WHERE workspace_id = #{workspaceId} AND id = #{id}
  </update>

  <update id="markRunning">
    UPDATE campaign_send
    SET status = 'running', started_at = COALESCE(started_at, CURRENT_TIMESTAMP)
    WHERE workspace_id = #{workspaceId} AND id = #{id} AND status = 'queued'
  </update>

  <update id="markCompleted">
    UPDATE campaign_send
    SET status = 'completed', completed_at = CURRENT_TIMESTAMP
    WHERE workspace_id = #{workspaceId} AND id = #{id} AND status = 'running'
  </update>

  <!-- One statement proves that nothing is left pending or dispatching and completes the send, so a
       live worker's terminal write cannot land between the proof and the completion. The counter
       refresh that follows a completion therefore reads every delivery in its final state: no row
       can return to pending or dispatching once the send is completed. -->
  <update id="markSettledAudienceSendCompleted">
    UPDATE campaign_send s
    SET s.status = 'completed', s.completed_at = CURRENT_TIMESTAMP
    WHERE s.workspace_id = #{workspaceId} AND s.id = #{id}
      AND s.status = 'running' AND s.origin = 'audience'
      AND NOT EXISTS (
        SELECT 1 FROM campaign_delivery d
        WHERE d.workspace_id = s.workspace_id AND d.send_id = s.id
          AND d.status IN ('pending', 'dispatching'))
  </update>

  <update id="refreshCounters">
    UPDATE campaign_send s
    SET s.total_recipients = (
          SELECT COUNT(*) FROM campaign_delivery d
          WHERE d.workspace_id = s.workspace_id AND d.send_id = s.id),
        s.dispatched_count = (
          SELECT COUNT(*) FROM campaign_delivery d
          WHERE d.workspace_id = s.workspace_id AND d.send_id = s.id AND d.status = 'dispatched'),
        s.skipped_count = (
          SELECT COUNT(*) FROM campaign_delivery d
          WHERE d.workspace_id = s.workspace_id AND d.send_id = s.id AND d.status = 'skipped'),
        s.failed_count = (
          SELECT COUNT(*) FROM campaign_delivery d
          WHERE d.workspace_id = s.workspace_id AND d.send_id = s.id AND d.status = 'failed')
    WHERE s.workspace_id = #{workspaceId} AND s.id = #{id}
  </update>

  <select id="queuedSendIds" resultType="int">
    SELECT id FROM campaign_send
    WHERE workspace_id = #{workspaceId}
      AND (status IN ('queued', 'running')
        OR (#{triggeredSendEnabled} AND status = 'triggered' AND EXISTS (
          SELECT 1 FROM campaign_delivery d
          WHERE d.workspace_id = campaign_send.workspace_id
            AND d.send_id = campaign_send.id
            AND d.status = 'pending')))
    ORDER BY id
  </select>

  <!-- An audience send left running with nothing pending or dispatching has not been settled, whoever
       left it that way: that evidence lives in the send and its own deliveries, so it survives a
       provider webhook or an operator resolution clearing the reconciliation marker the sweep wrote
       (applyProviderStatus and resolveReconciliation both clear reconciliation_required_at, and the
       webhook also moves the row out of failed). A send that is no longer running is only revisited
       for a failed counter that disagrees with its failed rows while it still owns an unresolved
       reconciliation row, which keeps that branch tied to sweep work. A dispatching row may belong to
       a live worker, whose own settlement completes the send. Settling it needs no provider. Both
       branches stay inside one workspace's sends (idx_campaign_send_status). -->
  <select id="audienceSendsAwaitingRecoverySettlement" resultType="int">
    SELECT s.id FROM campaign_send s
    WHERE s.workspace_id = #{workspaceId} AND s.origin = 'audience'
      AND ((s.status = 'running' AND NOT EXISTS (
            SELECT 1 FROM campaign_delivery p
            WHERE p.workspace_id = s.workspace_id AND p.send_id = s.id
              AND p.status IN ('pending', 'dispatching')))
        OR (s.failed_count &lt;&gt; (
            SELECT COUNT(*) FROM campaign_delivery f
            WHERE f.workspace_id = s.workspace_id AND f.send_id = s.id AND f.status = 'failed')
          AND EXISTS (
            SELECT 1 FROM campaign_delivery r
            WHERE r.workspace_id = s.workspace_id AND r.send_id = s.id AND r.status = 'failed'
              AND r.reconciliation_required_at IS NOT NULL)))
    ORDER BY s.id
    LIMIT #{limit}
  </select>

  <select id="workspaceIdsWithQueuedSends" resultType="int">
    SELECT DISTINCT workspace_id FROM campaign_send
    WHERE status IN ('queued', 'running')
      OR (status = 'triggered' AND EXISTS (
        SELECT 1 FROM campaign_delivery d
        WHERE d.workspace_id = campaign_send.workspace_id
          AND d.send_id = campaign_send.id
          AND ((#{triggeredSendEnabled} AND d.status = 'pending')
            OR (d.status = 'dispatching'
              AND d.dispatch_lease_until &lt; UTC_TIMESTAMP(6)))))
      OR (origin = 'audience' AND EXISTS (
        SELECT 1 FROM campaign_delivery d
        WHERE d.workspace_id = campaign_send.workspace_id
          AND d.send_id = campaign_send.id
          AND d.status = 'dispatching'
          AND d.dispatch_lease_owner IS NULL
          AND d.submitted_at IS NULL
          AND d.frequency_reserved_at &lt; DATE_SUB(UTC_TIMESTAMP(6),
            INTERVAL #{audienceReservationGraceMicros} MICROSECOND)))
      OR (origin = 'audience'
        AND failed_count &lt;&gt; (
          SELECT COUNT(*) FROM campaign_delivery f
          WHERE f.workspace_id = campaign_send.workspace_id
            AND f.send_id = campaign_send.id
            AND f.status = 'failed')
        AND EXISTS (
          SELECT 1 FROM campaign_delivery r
          WHERE r.workspace_id = campaign_send.workspace_id
            AND r.send_id = campaign_send.id
            AND r.status = 'failed'
            AND r.reconciliation_required_at IS NOT NULL))
  </select>

</mapper>
