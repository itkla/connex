<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="ooo.klae.connex.backend.mappers.PersonLifecycleHistoryMapper">

  <resultMap id="PersonLifecycleHistoryResult" type="PersonLifecycleHistory">
    <id     property="id"          column="id"/>
    <result property="workspaceId" column="workspace_id"/>
    <result property="personId"    column="person_id"/>
    <result property="fromStage"   column="from_stage"/>
    <result property="toStage"     column="to_stage"/>
    <result property="reason"      column="reason"/>
    <result property="note"        column="note"/>
    <result property="changedById" column="changed_by_id"/>
    <result property="changedAt"   column="changed_at"/>
  </resultMap>

  <insert id="insert" useGeneratedKeys="true" keyProperty="id">
    INSERT INTO person_lifecycle_history
      (workspace_id, person_id, from_stage, to_stage, reason, note, changed_by_id)
    VALUES (
      #{workspaceId}, #{personId},
      #{fromStage,jdbcType=VARCHAR}, #{toStage,jdbcType=VARCHAR},
      #{reason,jdbcType=VARCHAR}, #{note,jdbcType=VARCHAR},
      #{changedById,jdbcType=INTEGER})
  </insert>

  <select id="getByPersonId" resultMap="PersonLifecycleHistoryResult">
    SELECT id, workspace_id, person_id, from_stage, to_stage, reason, note, changed_by_id, changed_at
    FROM person_lifecycle_history
    WHERE workspace_id = #{workspaceId} AND person_id = #{personId}
    ORDER BY changed_at DESC, id DESC
    LIMIT #{limit}
  </select>
</mapper>
