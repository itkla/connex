<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="ooo.klae.connex.backend.mappers.ScheduleMapper">

  <sql id="columns">
    id, workspace_id, report_definition_id, cadence, recipient_user_ids, timezone,
    hour_of_day, enabled, run_as_user_id, next_run_at, last_run_at, created_by,
    created_at, updated_at
  </sql>

  <select id="getByReport" resultType="ReportSchedule">
    SELECT <include refid="columns"/> FROM report_schedule
    WHERE workspace_id = #{workspaceId} AND report_definition_id = #{reportDefinitionId}
  </select>

  <select id="getById" resultType="ReportSchedule">
    SELECT <include refid="columns"/> FROM report_schedule
    WHERE workspace_id = #{workspaceId} AND id = #{id}
  </select>

  <select id="lockById" resultType="ReportSchedule">
    SELECT <include refid="columns"/> FROM report_schedule
    WHERE workspace_id = #{workspaceId} AND id = #{id}
    FOR UPDATE
  </select>

  <insert id="insert" useGeneratedKeys="true" keyProperty="id">
    INSERT INTO report_schedule
      (workspace_id, report_definition_id, cadence, recipient_user_ids, timezone,
       hour_of_day, enabled, run_as_user_id, next_run_at, created_by)
    VALUES
      (#{workspaceId}, #{reportDefinitionId}, #{cadence}, #{recipientUserIds}, #{timezone},
       #{hourOfDay}, #{enabled}, #{runAsUserId}, #{nextRunAt}, #{createdBy})
  </insert>

  <update id="update">
    UPDATE report_schedule
    SET cadence = #{cadence}, recipient_user_ids = #{recipientUserIds},
        timezone = #{timezone}, hour_of_day = #{hourOfDay}, enabled = #{enabled},
        run_as_user_id = #{runAsUserId}, next_run_at = #{nextRunAt}
    WHERE workspace_id = #{workspaceId} AND id = #{id}
  </update>

  <delete id="deleteByReport">
    DELETE FROM report_schedule
    WHERE workspace_id = #{workspaceId} AND report_definition_id = #{reportDefinitionId}
  </delete>

  <select id="dueScheduleRefs" resultType="ooo.klae.connex.backend.dto.ReportScheduleRef">
    SELECT workspace_id, id AS schedule_id
    FROM report_schedule
    WHERE enabled = TRUE AND next_run_at &lt;= #{now}
    ORDER BY next_run_at, id
  </select>

  <update id="markClaimed">
    UPDATE report_schedule
    SET next_run_at = #{nextRunAt}, last_run_at = #{lastRunAt}
    WHERE workspace_id = #{workspaceId} AND id = #{id}
  </update>

  <update id="markSkipped">
    UPDATE report_schedule
    SET next_run_at = #{nextRunAt}
    WHERE workspace_id = #{workspaceId} AND id = #{id}
  </update>

</mapper>
