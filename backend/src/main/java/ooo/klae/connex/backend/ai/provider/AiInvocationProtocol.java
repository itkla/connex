package ooo.klae.connex.backend.ai.provider;

/** Closed metadata-only protocol diagnostic for one provider invocation. */
public enum AiInvocationProtocol {
    STANDARD,
    JSON_REACT,
    NATIVE_TOOLS
}
