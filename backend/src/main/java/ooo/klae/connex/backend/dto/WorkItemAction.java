package ooo.klae.connex.backend.dto;

/** Source-owned actions exposed by a work item. */
public enum WorkItemAction {
    complete,
    snooze,
    dismiss,
    approve,
    reject,
    open_context
}
