package ooo.klae.connex.backend.ai.provider;

/** Provider protocol used to acquire display-only model reasoning for one completion. */
public enum AiReasoningMode {
    NONE,
    TAGGED,
    NATIVE
}
