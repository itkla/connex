package ooo.klae.connex.backend.dto;

/** Structured evidence codes attached to projected work. */
public enum WorkItemEvidenceCode {
    task_due,
    task_open,
    deal_close_date,
    approval_requested
}
