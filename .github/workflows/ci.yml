name: CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  backend:
    name: Backend — build & test
    runs-on: ubuntu-latest
    services:
      mysql:
        image: mysql:9.7
        env:
          MYSQL_ROOT_PASSWORD: rootpassword
          MYSQL_DATABASE: connexdb
          MYSQL_USER: connexuser
          MYSQL_PASSWORD: connexpassword
        ports:
          - 3306:3306
        options: >-
          --health-cmd="mysqladmin ping -h 127.0.0.1 -uroot -prootpassword"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=15
    env:
      CONNEX_DB_URL: jdbc:mysql://127.0.0.1:3306/connexdb
      CONNEX_DB_USERNAME: connexuser
      CONNEX_DB_PASSWORD: connexpassword
    steps:
      - uses: actions/checkout@v4
      - name: Set up JDK 26
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '26'
          cache: gradle
      - name: Allow non-SUPER routine creation under binary logging
        run: mysql -h 127.0.0.1 -P 3306 -uroot -prootpassword -e "SET GLOBAL log_bin_trust_function_creators = 1;"
      - name: Test (runs Flyway migrations, arch tests, tenant-isolation tests)
        working-directory: backend
        run: bash gradlew test --no-daemon --stacktrace

  frontend:
    name: Frontend — typecheck & lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Set up pnpm
        uses: pnpm/action-setup@v4
        with:
          package_json_file: frontend/package.json
      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: pnpm
          cache-dependency-path: frontend/pnpm-lock.yaml
      - name: Install
        working-directory: frontend
        run: pnpm install --frozen-lockfile --ignore-scripts
      - name: Typecheck
        working-directory: frontend
        run: node_modules/.bin/tsc --noEmit
      - name: Lint changed files
        working-directory: frontend
        env:
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.before }}
        run: |
          if [ -z "$BASE_SHA" ] || [ "$BASE_SHA" = "0000000000000000000000000000000000000000" ]; then
            echo "No base commit to diff against — skipping changed-file lint."
            exit 0
          fi
          CHANGED=$(git diff --name-only --relative --diff-filter=ACMR "$BASE_SHA"...HEAD -- '*.ts' '*.tsx' || true)
          if [ -z "$CHANGED" ]; then
            echo "No changed frontend TS/TSX files — nothing to lint."
            exit 0
          fi
          echo "Linting changed files:"
          echo "$CHANGED"
          node_modules/.bin/eslint $CHANGED
