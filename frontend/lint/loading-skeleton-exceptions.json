{
    "highWaterMark": 10,
    "exceptions": [
        {
            "route": "/account",
            "kind": "redirect-only",
            "reason": "Renders nothing: redirects straight to /account/profile, which owns the skeleton the reader actually sees."
        },
        {
            "route": "/account/connections/reviews",
            "kind": "resolver-redirect",
            "reason": "A stable address for the capture review queue. It resolves the reader's own connected provider and redirects into /account/connections, whose skeleton already stands in for the panel it lands on; its only non-redirect outcome is CapabilityUnavailablePage, a terminal explanation that no skeleton should promise."
        },
        {
            "route": "/organization",
            "kind": "redirect-only",
            "reason": "Renders nothing: redirects straight to /organization/overview, which owns the skeleton."
        },
        {
            "route": "/overview/reports/[id]/snapshots",
            "kind": "redirect-only",
            "reason": "The bare snapshots path has no surface of its own; it redirects to the report, which lists every snapshot and owns the skeleton."
        },
        {
            "route": "/overview/reports/[id]/snapshots/[snapshotId]",
            "kind": "shares-parent-skeleton",
            "reason": "A frozen snapshot renders the same report document board as /overview/reports/[id]; that parent skeleton is already the correct mirror, and a second copy would only drift from it."
        },
        {
            "route": "/settings",
            "kind": "redirect-only",
            "reason": "Renders nothing: redirects straight to /settings/members, which owns the skeleton."
        },
        {
            "route": "/settings/membership",
            "kind": "redirect-only",
            "reason": "Renders nothing: redirects to /account/invites, the canonical destination for the membership job."
        },
        {
            "route": "/settings/notifications",
            "kind": "redirect-only",
            "reason": "Renders nothing: redirects to /account/notifications, the canonical destination for notification preferences."
        },
        {
            "route": "/settings/security",
            "kind": "redirect-only",
            "reason": "Renders nothing: redirects to /account/security, the canonical destination for the security job."
        },
        {
            "route": "/settings/workflows/[legacyRuleId]",
            "kind": "resolver-redirect",
            "reason": "The permanent redirect from an old automation link. It resolves the workflow id and forwards to /workflows/[workflowId], whose editor shell owns the skeleton."
        }
    ]
}
